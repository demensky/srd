export interface ProprietaryMapItemOptions {
  clickable?: boolean;
  draggable?: boolean;
  fillColor?: string;
  fillOpacity?: number;
  // TODO: map?: GoogleMap;
  visible?: boolean;
  zIndex?: number;
}
